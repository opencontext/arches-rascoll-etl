CREATE SCHEMA IF NOT EXISTS "staging";


create or replace function __arches_tile_view_update() returns trigger as $$
declare
    view_namespace text;
    group_id uuid;
    graph_id uuid;
    parent_id uuid;
    tile_id uuid;
    transaction_id uuid;
    json_data json;
    old_json_data jsonb;
    edit_type text;
begin
    select graphid into graph_id from nodes where nodeid = group_id;
    view_namespace = format('%s.%s', tg_table_schema, tg_table_name);
    select obj_description(view_namespace::regclass, 'pg_class') into group_id;
    if (TG_OP = 'DELETE') then
        select tiledata into old_json_data from tiles where tileid = old.tileid;
        delete from resource_x_resource where tileid = old.tileid;
        delete from public.tiles where tileid = old.tileid;
        insert into bulk_index_queue (resourceinstanceid, createddate)
            values (old.resourceinstanceid, current_timestamp) on conflict do nothing;
        insert into edit_log (
            resourceclassid,
            resourceinstanceid,
            nodegroupid,
            tileinstanceid,
            edittype,
            oldvalue,
            timestamp,
            note,
            transactionid
        ) values (
            graph_id,
            old.resourceinstanceid,
            group_id,
            old.tileid,
            'tile delete',
            old_json_data,
            now(),
            'loaded via SQL backend',
            public.uuid_generate_v1mc()
        );
        return old;
    else
        select __arches_get_json_data_for_view(new, tg_table_schema, tg_table_name) into json_data;
        select __arches_get_parent_id_for_view(new, tg_table_schema, tg_table_name) into parent_id;
        tile_id = new.tileid;
        if (new.transactionid is null) then
            transaction_id = public.uuid_generate_v1mc();
        else
            transaction_id = new.transactionid;
        end if;

        if (TG_OP = 'UPDATE') then
            select tiledata into old_json_data from tiles where tileid = tile_id;
            edit_type = 'tile edit';
            if (transaction_id = old.transactionid) then
                transaction_id = public.uuid_generate_v1mc();
            end if;
            update public.tiles
            set tiledata = json_data,
                nodegroupid = group_id,
                parenttileid = parent_id,
                resourceinstanceid = new.resourceinstanceid
            where tileid = new.tileid;
        elsif (TG_OP = 'INSERT') then
            old_json_data = null;
            edit_type = 'tile create';
            if tile_id is null then
                tile_id = public.uuid_generate_v1mc();
            end if;
            insert into public.tiles(
                tileid,
                tiledata,
                nodegroupid,
                parenttileid,
                resourceinstanceid
            ) values (
                tile_id,
                json_data,
                group_id,
                parent_id,
                new.resourceinstanceid
            );
        end if;
        perform __arches_refresh_tile_resource_relationships(tile_id);
        insert into bulk_index_queue (resourceinstanceid, createddate)
            values (new.resourceinstanceid, current_timestamp) on conflict do nothing;
        insert into edit_log (
            resourceclassid,
            resourceinstanceid,
            nodegroupid,
            tileinstanceid,
            edittype,
            newvalue,
            oldvalue,
            timestamp,
            note,
            transactionid
        ) values (
            graph_id,
            new.resourceinstanceid,
            group_id,
            tile_id,
            edit_type,
            json_data::jsonb,
            old_json_data,
            now(),
            'loaded via SQL backend',
            transaction_id
        );
        return new;
    end if;
    end;
$$ language plpgsql;



UPDATE node_groups
SET cardinality = 'n'
WHERE nodegroupid = '6ee83594-08e4-11f0-81c1-0275dc2ded29';



UPDATE node_groups
SET cardinality = 'n'
WHERE nodegroupid = '3aff54bc-0f3b-11f0-aa84-02460e9d2217';



-- FUNCTION: public.__arches_instance_view_update()

-- DROP FUNCTION IF EXISTS public.__arches_instance_view_update();

CREATE OR REPLACE FUNCTION public.__arches_instance_view_update()
    RETURNS trigger
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE NOT LEAKPROOF
AS $BODY$
                declare
                    view_namespace text;
                    model_id uuid;
                    instance_id uuid;
                    transaction_id uuid;
                    edit_type text;
                begin
                    view_namespace = format('%s.%s', tg_table_schema, tg_table_name);
                    select obj_description(view_namespace::regclass, 'pg_class') into model_id;
                    if (TG_OP = 'DELETE') then
                        delete from public.resource_instances where resourceinstanceid = old.resourceinstanceid;
                        insert into bulk_index_queue (resourceinstanceid, createddate)
                            values (old.resourceinstanceid, current_timestamp) on conflict do nothing;
                        insert into edit_log (
                            resourceclassid,
                            resourceinstanceid,
                            edittype,
                            timestamp,
                            note,
                            transactionid
                        ) values (
                            model_id,
                            old.resourceinstanceid,
                            'delete',
                            now(),
                            'loaded via SQL backend',
                            public.uuid_generate_v1mc()
                        );
                        return old;
                    else
                        instance_id = new.resourceinstanceid;
                        if instance_id is null then
                            instance_id = public.uuid_generate_v1mc();
                        end if;

                        if (new.transactionid is null) then
                            transaction_id = public.uuid_generate_v1mc();
                        else
                            transaction_id = new.transactionid;
                        end if;

                        if (TG_OP = 'UPDATE') then
                            edit_type = 'edit';
                            if (transaction_id = old.transactionid) then
                                transaction_id = public.uuid_generate_v1mc();
                            end if;
                            update public.resource_instances
                            set createdtime = new.createdtime,
                                legacyid = new.legacyid
                            where resourceinstanceid = instance_id;
                        elsif (TG_OP = 'INSERT') then
                            edit_type = 'create';
                            insert into public.resource_instances(
                                resourceinstanceid,
                                graphid,
                                legacyid,
                                createdtime,
								 resource_instance_lifecycle_state_id
                            ) values (
                                instance_id,
                                model_id,
                                new.legacyid,
                                now(),
								'f75bb034-36e3-4ab4-8167-f520cf0b4c58'::uuid
                            );
                        end if;
                        insert into bulk_index_queue (resourceinstanceid, createddate)
                            values (instance_id, current_timestamp) on conflict do nothing;
                        insert into edit_log (
                            resourceclassid,
                            resourceinstanceid,
                            edittype,
                            timestamp,
                            note,
                            transactionid
                        ) values (
                            model_id,
                            instance_id,
                            edit_type,
                            now(),
                            'loaded via SQL backend',
                            transaction_id
                        );
                        return new;
                    end if;
                end;
            
$BODY$;

ALTER FUNCTION public.__arches_instance_view_update()
    OWNER TO postgres;



    SELECT __arches_create_resource_model_views('bda239c6-d376-11ef-a239-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('3dda9f54-d771-11ef-825b-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('36956e50-d770-11ef-8f5d-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('e1d0ea1a-d770-11ef-8c40-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('26a55ac6-d772-11ef-825b-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('da0ed58e-d771-11ef-af99-0275dc2ded29');
    


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci
                WHERE (staging.rsci.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type_, 
name_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
common_name_tileid::uuid, 
common_name_name_content::jsonb, 
common_name_name_type_::uuid[], 
common_name_name_language_::uuid[], 
common_name_nodegroupid::uuid
                
                FROM staging.rsci
                WHERE (staging.rsci.common_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci.common_name_tileid::uuid IS NOT NULL) AND 
((staging.rsci.common_name_name_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type_, 
name_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
additional_names_tileid::uuid, 
additional_names_name_content::jsonb, 
additional_names_name_type_::uuid[], 
additional_names_name_language_::uuid[], 
additional_names_nodegroupid::uuid
                
                FROM staging.rsci
                WHERE (staging.rsci.additional_names_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci.additional_names_tileid::uuid IS NOT NULL) AND 
((staging.rsci.additional_names_name_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.identifier (
                    resourceinstanceid, 
tileid, 
identifier_content, 
identifier_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
barcode_no_tileid::uuid, 
barcode_no_identifier_content::jsonb, 
barcode_no_identifier_type::uuid[], 
barcode_no_nodegroupid::uuid
                
                FROM staging.rsci
                WHERE (staging.rsci.barcode_no_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci.barcode_no_tileid::uuid IS NOT NULL) AND 
((staging.rsci.barcode_no_identifier_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.facet_type (
                    resourceinstanceid, 
tileid, 
facet_type, 
facet_type_metatype, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
facet_type::uuid, 
facet_type_metatype::uuid, 
nodegroupid::uuid
                
                FROM staging.rsci
                WHERE (staging.rsci.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci.tileid::uuid IS NOT NULL) AND 
((staging.rsci.facet_type::uuid IS NOT NULL))
                ORDER BY staging.rsci.resourceinstanceid
                
                ;
                


                INSERT INTO place.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.place
                WHERE (staging.place.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM place.instances)) AND 
((staging.place.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.place.resourceinstanceid
                
                ;
                


                INSERT INTO place.name (
                    resourceinstanceid, 
tileid, 
content, 
type, 
language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
specific_place_tileid::uuid, 
specific_place_content::jsonb, 
specific_place_type::uuid[], 
specific_place_language::uuid[], 
specific_place_nodegroupid::uuid
                
                FROM staging.place
                WHERE (staging.place.specific_place_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.place.specific_place_tileid::uuid IS NOT NULL) AND 
((staging.place.specific_place_content::jsonb IS NOT NULL))
                ORDER BY staging.place.resourceinstanceid
                
                ;
                


                INSERT INTO place.statement (
                    resourceinstanceid, 
tileid, 
content, 
type, 
language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
statement_tileid::uuid, 
statement_content::jsonb, 
statement_type::uuid[], 
statement_language::uuid[], 
statement_nodegroupid::uuid
                
                FROM staging.place
                WHERE (staging.place.statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.place.statement_tileid::uuid IS NOT NULL) AND 
((staging.place.statement_content::jsonb IS NOT NULL))
                ORDER BY staging.place.resourceinstanceid
                
                ;
                


                INSERT INTO place.external_uri (
                    resourceinstanceid, 
tileid, 
external_uri, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
specific_place_uri_tileid::uuid, 
specific_place_uri_external_uri::jsonb, 
specific_place_uri_nodegroupid::uuid
                
                FROM staging.place
                WHERE (staging.place.specific_place_uri_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.place.specific_place_uri_tileid::uuid IS NOT NULL) AND 
((staging.place.specific_place_uri_external_uri::jsonb IS NOT NULL))
                ORDER BY staging.place.resourceinstanceid
                
                ;
                


                INSERT INTO place.defined_by (
                    resourceinstanceid, 
tileid, 
defined_by, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
geo_point_tileid::uuid, 
ST_AsText(ST_GeomFromGeoJSON(geo_point_defined_by)), 
geo_point_nodegroupid::uuid
                
                FROM staging.place
                WHERE (staging.place.geo_point_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.place.geo_point_tileid::uuid IS NOT NULL) AND 
((staging.place.geo_point_defined_by::jsonb IS NOT NULL))
                ORDER BY staging.place.resourceinstanceid
                
                ;
                


                INSERT INTO group_.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.group_
                WHERE (staging.group_.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM group_.instances)) AND 
((staging.group_.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.group_.resourceinstanceid
                
                ;
                


                INSERT INTO group_.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type, 
name_language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
group_name_tileid::uuid, 
group_name_name_content::jsonb, 
group_name_name_type::uuid[], 
group_name_name_language::uuid[], 
group_name_nodegroupid::uuid
                
                FROM staging.group_
                WHERE (staging.group_.group_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.group_.group_name_tileid::uuid IS NOT NULL) AND 
((staging.group_.group_name_name_content::jsonb IS NOT NULL))
                ORDER BY staging.group_.resourceinstanceid
                
                ;
                


                INSERT INTO person.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.person
                WHERE (staging.person.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM person.instances)) AND 
((staging.person.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.person.resourceinstanceid
                
                ;
                


                INSERT INTO person.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type, 
name_language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
person_name_tileid::uuid, 
person_name_name_content::jsonb, 
person_name_name_type::uuid[], 
person_name_name_language::uuid[], 
person_name_nodegroupid::uuid
                
                FROM staging.person
                WHERE (staging.person.person_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.person.person_name_tileid::uuid IS NOT NULL) AND 
((staging.person.person_name_name_content::jsonb IS NOT NULL))
                ORDER BY staging.person.resourceinstanceid
                
                ;
                


                INSERT INTO collection_or_set.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.collection_or_set
                WHERE (staging.collection_or_set.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM collection_or_set.instances)) AND 
((staging.collection_or_set.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.collection_or_set.resourceinstanceid
                
                ;
                


                INSERT INTO collection_or_set.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type, 
name_language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
name_content::jsonb, 
name_type::uuid[], 
name_language::uuid[], 
nodegroupid::uuid
                
                FROM staging.collection_or_set
                WHERE (staging.collection_or_set.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.collection_or_set.tileid::uuid IS NOT NULL) AND 
((staging.collection_or_set.name_content::jsonb IS NOT NULL))
                ORDER BY staging.collection_or_set.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_statements
                WHERE (staging.rsci_statements.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_statements.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_statements.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.statement (
                    resourceinstanceid, 
tileid, 
statement_content, 
statement_type, 
statement_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
notes_tileid::uuid, 
notes_statement_content::jsonb, 
notes_statement_type::uuid[], 
notes_statement_language_::uuid[], 
notes_nodegroupid::uuid
                
                FROM staging.rsci_statements
                WHERE (staging.rsci_statements.notes_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_statements.notes_tileid::uuid IS NOT NULL) AND 
((staging.rsci_statements.notes_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_statements.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.statement (
                    resourceinstanceid, 
tileid, 
statement_content, 
statement_type, 
statement_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
physical_form_tileid::uuid, 
physical_form_statement_content::jsonb, 
physical_form_statement_type::uuid[], 
physical_form_statement_language_::uuid[], 
physical_form_nodegroupid::uuid
                
                FROM staging.rsci_statements
                WHERE (staging.rsci_statements.physical_form_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_statements.physical_form_tileid::uuid IS NOT NULL) AND 
((staging.rsci_statements.physical_form_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_statements.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_group_safety
                WHERE (staging.rsci_group_safety.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_group_safety.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_group_safety.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.fire_safety_classification (
                    resourceinstanceid, 
tileid, 
fire_safety_classification_type, 
nodegroupid, 
fire_safety_classification_hold_for
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
fire_safety_classification_type::uuid, 
nodegroupid::uuid, 

                        coalesce(
                            case jsonb_typeof(related_objs) 
                                when 'array' then related_objs 
                                else '[]'::jsonb end
                            ) as related_objs_1
                        
                
                FROM staging.rsci_group_safety
                WHERE (staging.rsci_group_safety.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_group_safety.tileid::uuid IS NOT NULL) AND 
((staging.rsci_group_safety.fire_safety_classification_type::uuid IS NOT NULL) OR (staging.rsci_group_safety.related_objs::jsonb IS NOT NULL))
                ORDER BY staging.rsci_group_safety.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.health_safety_classification (
                    resourceinstanceid, 
tileid, 
health_safety_classification_type, 
nodegroupid, 
health_safety_classification_hold_for
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
health_safety_classification_type::uuid, 
nodegroupid::uuid, 

                        coalesce(
                            case jsonb_typeof(related_objs) 
                                when 'array' then related_objs 
                                else '[]'::jsonb end
                            ) as related_objs_1
                        
                
                FROM staging.rsci_group_safety
                WHERE (staging.rsci_group_safety.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_group_safety.tileid::uuid IS NOT NULL) AND 
((staging.rsci_group_safety.health_safety_classification_type::uuid IS NOT NULL) OR (staging.rsci_group_safety.related_objs::jsonb IS NOT NULL))
                ORDER BY staging.rsci_group_safety.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.general_safety_classification (
                    resourceinstanceid, 
tileid, 
general_safety_classification_type, 
nodegroupid, 
general_safety_classification_hold_for
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
general_safety_classification_type::uuid, 
nodegroupid::uuid, 

                        coalesce(
                            case jsonb_typeof(related_objs) 
                                when 'array' then related_objs 
                                else '[]'::jsonb end
                            ) as related_objs_1
                        
                
                FROM staging.rsci_group_safety
                WHERE (staging.rsci_group_safety.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_group_safety.tileid::uuid IS NOT NULL) AND 
((staging.rsci_group_safety.general_safety_classification_type::uuid IS NOT NULL) OR (staging.rsci_group_safety.related_objs::jsonb IS NOT NULL))
                ORDER BY staging.rsci_group_safety.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.reactivity_safety_classification (
                    resourceinstanceid, 
tileid, 
reactivity_safety_classification_type, 
nodegroupid, 
reactivity_safety_classification_hold_for
                ) SELECT
                    resourceinstanceid::uuid, 
reactivity_safety_tileid::uuid, 
reactivity_safety_reactivity_safety_classification_type::uuid, 
reactivity_safety_nodegroupid::uuid, 

                        coalesce(
                            case jsonb_typeof(reactivity_safety_related_objs) 
                                when 'array' then reactivity_safety_related_objs 
                                else '[]'::jsonb end
                            ) as reactivity_safety_related_objs_1
                        
                
                FROM staging.rsci_group_safety
                WHERE (staging.rsci_group_safety.reactivity_safety_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_group_safety.reactivity_safety_tileid::uuid IS NOT NULL) AND 
((staging.rsci_group_safety.reactivity_safety_reactivity_safety_classification_type::uuid IS NOT NULL) OR (staging.rsci_group_safety.reactivity_safety_related_objs::jsonb IS NOT NULL))
                ORDER BY staging.rsci_group_safety.resourceinstanceid
                
                ;
                


                INSERT INTO provenance_activity.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.provenance_activity
                WHERE (staging.provenance_activity.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM provenance_activity.instances)) AND 
((staging.provenance_activity.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.provenance_activity.resourceinstanceid
                
                ;
                


                INSERT INTO provenance_activity.provenance_activity_name (
                    resourceinstanceid, 
tileid, 
content, 
type, 
language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
prov_act_name_tileid::uuid, 
prov_act_name_content::jsonb, 
prov_act_name_type::uuid[], 
prov_act_name_language::uuid[], 
prov_act_name_nodegroupid::uuid
                
                FROM staging.provenance_activity
                WHERE (staging.provenance_activity.prov_act_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.provenance_activity.prov_act_name_tileid::uuid IS NOT NULL) AND 
((staging.provenance_activity.prov_act_name_content::jsonb IS NOT NULL))
                ORDER BY staging.provenance_activity.resourceinstanceid
                
                ;
                


                INSERT INTO provenance_activity.acquisition (
                    resourceinstanceid, 
tileid, 
_label, 
nodegroupid, 
carried_out_by, 
transferred_title_from, 
transferred_title_of
                ) SELECT
                    resourceinstanceid::uuid, 
acquisition_tileid::uuid, 
acquisition__label::jsonb, 
acquisition_nodegroupid::uuid, 

                        coalesce(
                            case jsonb_typeof(acquisition_carried_out_by_related_objs) 
                                when 'array' then acquisition_carried_out_by_related_objs 
                                else '[]'::jsonb end
                            ) as acquisition_carried_out_by_related_objs_1
                        , 

                        coalesce(
                            case jsonb_typeof(acquisition_transferred_title_from_related_objs) 
                                when 'array' then acquisition_transferred_title_from_related_objs 
                                else '[]'::jsonb end
                            ) as acquisition_transferred_title_from_related_objs_5
                        , 

                        coalesce(
                            case jsonb_typeof(acquisition_transferred_title_of_related_objs) 
                                when 'array' then acquisition_transferred_title_of_related_objs 
                                else '[]'::jsonb end
                            ) as acquisition_transferred_title_of_related_objs_7
                        
                
                FROM staging.provenance_activity
                WHERE (staging.provenance_activity.acquisition_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.provenance_activity.acquisition_tileid::uuid IS NOT NULL) AND 
((staging.provenance_activity.acquisition__label::jsonb IS NOT NULL) OR (staging.provenance_activity.acquisition_carried_out_by_related_objs::jsonb IS NOT NULL) OR (staging.provenance_activity.acquisition_transferred_title_from_related_objs::jsonb IS NOT NULL) OR (staging.provenance_activity.acquisition_transferred_title_of_related_objs::jsonb IS NOT NULL))
                ORDER BY staging.provenance_activity.resourceinstanceid
                
                ;
                


                INSERT INTO provenance_activity.acquisition_timespan (
                    resourceinstanceid, 
tileid, 
begin_of_the_begin, 
nodegroupid, 
acquisition, 
end_of_the_begin, 
begin_of_the_end, 
end_of_the_end
                ) SELECT
                    resourceinstanceid::uuid, 
acquisition_timespan_tileid::uuid, 
acquisition_timespan_begin_of_the_begin::timestamp, 
acquisition_timespan_nodegroupid::uuid, 
acquisition_tileid::uuid, 
acquisition_timespan_end_of_the_begin::timestamp, 
acquisition_timespan_begin_of_the_end::timestamp, 
acquisition_timespan_end_of_the_end::timestamp
                
                FROM staging.provenance_activity
                WHERE (staging.provenance_activity.acquisition_timespan_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.provenance_activity.acquisition_timespan_tileid::uuid IS NOT NULL) AND 
((staging.provenance_activity.acquisition_timespan_begin_of_the_begin::timestamp IS NOT NULL) OR (staging.provenance_activity.acquisition_timespan_end_of_the_begin::timestamp IS NOT NULL) OR (staging.provenance_activity.acquisition_timespan_begin_of_the_end::timestamp IS NOT NULL) OR (staging.provenance_activity.acquisition_timespan_end_of_the_end::timestamp IS NOT NULL))
                ORDER BY staging.provenance_activity.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_production
                WHERE (staging.rsci_production.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_production.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_production.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.production_ (
                    resourceinstanceid, 
tileid, 
production__label, 
nodegroupid, 
production_type, 
production_carried_out_by, 
production_location, 
production_location_geo
                ) SELECT
                    resourceinstanceid::uuid, 
prod_1_tileid::uuid, 
prod_1_production__label::jsonb, 
prod_1_nodegroupid::uuid, 
prod_1_production_type::uuid[], 

                        coalesce(
                            case jsonb_typeof(prod_1_production_carried_out_by_related_objs) 
                                when 'array' then prod_1_production_carried_out_by_related_objs 
                                else '[]'::jsonb end
                            ) as prod_1_production_carried_out_by_related_objs_1
                        , 

                        coalesce(
                            case jsonb_typeof(prod_1_production_location_related_objs) 
                                when 'array' then prod_1_production_location_related_objs 
                                else '[]'::jsonb end
                            ) as prod_1_production_location_related_objs_2
                        , 

                        CASE 
                            WHEN prod_1_production_location_geo IS NULL THEN NULL 
                            WHEN jsonb_typeof(prod_1_production_location_geo) = 'object' 
                            THEN ST_AsText(ST_GeomFromGeoJSON(prod_1_production_location_geo)) 
                            ELSE NULL 
                        END AS prod_1_production_location_geo_geom
                        
                
                FROM staging.rsci_production
                WHERE (staging.rsci_production.prod_1_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_production.prod_1_tileid::uuid IS NOT NULL) AND 
((staging.rsci_production.prod_1_production__label::jsonb IS NOT NULL) OR (staging.rsci_production.prod_1_production_carried_out_by_related_objs::jsonb IS NOT NULL) OR (staging.rsci_production.prod_1_production_location_related_objs::jsonb IS NOT NULL) OR (staging.rsci_production.prod_1_production_location_geo::jsonb IS NOT NULL))
                ORDER BY staging.rsci_production.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.production_time (
                    resourceinstanceid, 
tileid, 
production_time_begin_of_the_begin, 
nodegroupid, 
production_, 
production_time_end_of_the_begin, 
production_time_begin_of_the_end, 
production_time_end_of_the_end
                ) SELECT
                    resourceinstanceid::uuid, 
pt_tileid::uuid, 
pt_production_time_begin_of_the_begin::timestamp, 
pt_nodegroupid::uuid, 
prod_1_tileid::uuid, 
pt_production_time_end_of_the_begin::timestamp, 
pt_production_time_begin_of_the_end::timestamp, 
pt_production_time_end_of_the_end::timestamp
                
                FROM staging.rsci_production
                WHERE (staging.rsci_production.pt_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_production.pt_tileid::uuid IS NOT NULL) AND 
((staging.rsci_production.pt_production_time_begin_of_the_begin::timestamp IS NOT NULL) OR (staging.rsci_production.pt_production_time_end_of_the_begin::timestamp IS NOT NULL) OR (staging.rsci_production.pt_production_time_begin_of_the_end::timestamp IS NOT NULL) OR (staging.rsci_production.pt_production_time_end_of_the_end::timestamp IS NOT NULL))
                ORDER BY staging.rsci_production.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.production_statement (
                    resourceinstanceid, 
tileid, 
production_statement_content, 
production_statement_type, 
production_statement_language, 
nodegroupid, 
production_
                ) SELECT
                    resourceinstanceid::uuid, 
prod_statement_tileid::uuid, 
prod_statement_production_statement_content::jsonb, 
prod_statement_production_statement_type::uuid[], 
prod_statement_production_statement_language::uuid[], 
prod_statement_nodegroupid::uuid, 
prod_1_tileid::uuid
                
                FROM staging.rsci_production
                WHERE (staging.rsci_production.prod_statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_production.prod_statement_tileid::uuid IS NOT NULL) AND 
((staging.rsci_production.prod_statement_production_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_production.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_materials_object_types.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.material_data_assignment_name (
                    resourceinstanceid, 
tileid, 
material_data_assignment_name_content, 
material_data_assignment_name_type, 
material_data_assignment_name_language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
chem_tileid::uuid, 
chem_material_data_assignment_name_content::jsonb, 
chem_material_data_assignment_name_type::uuid[], 
chem_material_data_assignment_name_language::uuid[], 
chem_nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.chem_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.chem_tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.chem_material_data_assignment_name_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type_, 
name_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
gbif_name_tileid::uuid, 
gbif_name_name_content::jsonb, 
gbif_name_name_type_::uuid[], 
gbif_name_name_language_::uuid[], 
gbif_name_nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.gbif_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.gbif_name_tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.gbif_name_name_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.statement (
                    resourceinstanceid, 
tileid, 
statement_content, 
statement_type, 
statement_language_, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
gbif_tileid::uuid, 
gbif_statement_content::jsonb, 
gbif_statement_type::uuid[], 
gbif_statement_language_::uuid[], 
gbif_nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.gbif_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.gbif_tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.gbif_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.mixture_type (
                    resourceinstanceid, 
tileid, 
mixture_type, 
mixture_type_metatype, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
tileid::uuid, 
mixture_type::uuid[], 
mixture_type_metatype::uuid, 
nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.mixture_type::uuid[] IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.attribute_type (
                    resourceinstanceid, 
tileid, 
attribute_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
atribs_tileid::uuid, 
atribs_attribute_type::uuid[], 
atribs_nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.atribs_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.atribs_tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.atribs_attribute_type::uuid[] IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.material (
                    resourceinstanceid, 
tileid, 
material, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
mat_tileid::uuid, 
mat_material::uuid[], 
mat_nodegroupid::uuid
                
                FROM staging.rsci_materials_object_types
                WHERE (staging.rsci_materials_object_types.mat_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_materials_object_types.mat_tileid::uuid IS NOT NULL) AND 
((staging.rsci_materials_object_types.mat_material::uuid[] IS NOT NULL))
                ORDER BY staging.rsci_materials_object_types.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_components.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_part (
                    resourceinstanceid, 
tileid, 
part_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
comp1_tileid::uuid, 
comp1_part_type::uuid, 
comp1_nodegroupid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp1_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp1_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp1_part_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_statement (
                    resourceinstanceid, 
tileid, 
part_statement_content, 
part_statement_type, 
part_statement_language, 
nodegroupid, 
has_part
                ) SELECT
                    resourceinstanceid::uuid, 
comp1_statement_tileid::uuid, 
comp1_statement_part_statement_content::jsonb, 
comp1_statement_part_statement_type::uuid[], 
comp1_statement_part_statement_language::uuid[], 
comp1_statement_nodegroupid::uuid, 
comp1_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp1_statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp1_statement_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp1_statement_part_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_dimension (
                    resourceinstanceid, 
tileid, 
part_dimension_value_, 
part_dimension_type_, 
part_dimension_unit, 
nodegroupid, 
has_part
                ) SELECT
                    resourceinstanceid::uuid, 
comp1_n_tileid::uuid, 
comp1_n_part_dimension_value_::numeric, 
comp1_n_part_dimension_type_::uuid, 
comp1_n_part_dimension_unit::uuid, 
comp1_n_nodegroupid::uuid, 
comp1_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp1_n_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp1_n_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp1_n_part_dimension_value_::numeric IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_dimension_name (
                    resourceinstanceid, 
tileid, 
part_dimension_name_content_, 
part_dimension_name_type_, 
part_dimension_name_language_, 
nodegroupid, 
part_dimension
                ) SELECT
                    resourceinstanceid::uuid, 
comp1_p_tileid::uuid, 
comp1_p_part_dimension_name_content_::jsonb, 
comp1_p_part_dimension_name_type_::uuid[], 
comp1_p_part_dimension_name_language_::uuid[], 
comp1_p_nodegroupid::uuid, 
comp1_n_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp1_p_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp1_p_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp1_p_part_dimension_name_content_::jsonb IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_part (
                    resourceinstanceid, 
tileid, 
part_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
comp2_tileid::uuid, 
comp2_part_type::uuid, 
comp2_nodegroupid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp2_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp2_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp2_part_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_statement (
                    resourceinstanceid, 
tileid, 
part_statement_content, 
part_statement_type, 
part_statement_language, 
nodegroupid, 
has_part
                ) SELECT
                    resourceinstanceid::uuid, 
comp2_statement_tileid::uuid, 
comp2_statement_part_statement_content::jsonb, 
comp2_statement_part_statement_type::uuid[], 
comp2_statement_part_statement_language::uuid[], 
comp2_statement_nodegroupid::uuid, 
comp2_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp2_statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp2_statement_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp2_statement_part_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_dimension (
                    resourceinstanceid, 
tileid, 
part_dimension_value_, 
part_dimension_type_, 
part_dimension_unit, 
nodegroupid, 
has_part
                ) SELECT
                    resourceinstanceid::uuid, 
comp2_n_tileid::uuid, 
comp2_n_part_dimension_value_::numeric, 
comp2_n_part_dimension_type_::uuid, 
comp2_n_part_dimension_unit::uuid, 
comp2_n_nodegroupid::uuid, 
comp2_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp2_n_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp2_n_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp2_n_part_dimension_value_::numeric IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_dimension_name (
                    resourceinstanceid, 
tileid, 
part_dimension_name_content_, 
part_dimension_name_type_, 
part_dimension_name_language_, 
nodegroupid, 
part_dimension
                ) SELECT
                    resourceinstanceid::uuid, 
comp2_p_tileid::uuid, 
comp2_p_part_dimension_name_content_::jsonb, 
comp2_p_part_dimension_name_type_::uuid[], 
comp2_p_part_dimension_name_language_::uuid[], 
comp2_p_nodegroupid::uuid, 
comp2_n_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp2_p_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp2_p_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp2_p_part_dimension_name_content_::jsonb IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_part (
                    resourceinstanceid, 
tileid, 
part_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
comp3_tileid::uuid, 
comp3_part_type::uuid, 
comp3_nodegroupid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp3_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp3_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp3_part_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.part_statement (
                    resourceinstanceid, 
tileid, 
part_statement_content, 
part_statement_type, 
part_statement_language, 
nodegroupid, 
has_part
                ) SELECT
                    resourceinstanceid::uuid, 
comp3_statement_tileid::uuid, 
comp3_statement_part_statement_content::jsonb, 
comp3_statement_part_statement_type::uuid[], 
comp3_statement_part_statement_language::uuid[], 
comp3_statement_nodegroupid::uuid, 
comp3_tileid::uuid
                
                FROM staging.rsci_components
                WHERE (staging.rsci_components.comp3_statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_components.comp3_statement_tileid::uuid IS NOT NULL) AND 
((staging.rsci_components.comp3_statement_part_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_components.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_colors.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_color (
                    resourceinstanceid, 
tileid, 
color_name_content, 
color_name_type, 
color_name_language, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
color_tileid::uuid, 
color_color_name_content::jsonb, 
color_color_name_type::uuid[], 
color_color_name_language::uuid, 
color_nodegroupid::uuid
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.color_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_colors.color_tileid::uuid IS NOT NULL) AND 
((staging.rsci_colors.color_color_name_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_color (
                    resourceinstanceid, 
tileid, 
color_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
color_a_tileid::uuid, 
color_a_color_type::uuid, 
color_a_nodegroupid::uuid
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.color_a_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_colors.color_a_tileid::uuid IS NOT NULL) AND 
((staging.rsci_colors.color_a_color_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_color (
                    resourceinstanceid, 
tileid, 
color_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
color_b_tileid::uuid, 
color_b_color_type::uuid, 
color_b_nodegroupid::uuid
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.color_b_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_colors.color_b_tileid::uuid IS NOT NULL) AND 
((staging.rsci_colors.color_b_color_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_color (
                    resourceinstanceid, 
tileid, 
color_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
color_c_tileid::uuid, 
color_c_color_type::uuid, 
color_c_nodegroupid::uuid
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.color_c_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_colors.color_c_tileid::uuid IS NOT NULL) AND 
((staging.rsci_colors.color_c_color_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.has_color (
                    resourceinstanceid, 
tileid, 
color_type, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
color_d_tileid::uuid, 
color_d_color_type::uuid, 
color_d_nodegroupid::uuid
                
                FROM staging.rsci_colors
                WHERE (staging.rsci_colors.color_d_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_colors.color_d_tileid::uuid IS NOT NULL) AND 
((staging.rsci_colors.color_d_color_type::uuid IS NOT NULL))
                ORDER BY staging.rsci_colors.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id
                ) SELECT
                    resourceinstanceid::uuid, 
'7e3cce56-fbfb-4a4b-8e83-59b9f9e7cb75'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer
                
                FROM staging.rsci_current_location
                WHERE (staging.rsci_current_location.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM reference_and_sample_collection_item.instances)) AND 
((staging.rsci_current_location.resourceinstanceid::uuid IS NOT NULL))
                ORDER BY staging.rsci_current_location.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.current_location (
                    resourceinstanceid, 
tileid, 
current_location, 
nodegroupid
                ) SELECT
                    resourceinstanceid::uuid, 
cur_loc_tileid::uuid, 
cur_loc_current_location::jsonb, 
cur_loc_nodegroupid::uuid
                
                FROM staging.rsci_current_location
                WHERE (staging.rsci_current_location.cur_loc_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_current_location.cur_loc_tileid::uuid IS NOT NULL) AND 
((staging.rsci_current_location.cur_loc_current_location::jsonb IS NOT NULL))
                ORDER BY staging.rsci_current_location.resourceinstanceid
                
                ;
                


                INSERT INTO reference_and_sample_collection_item.current_location_statement (
                    resourceinstanceid, 
tileid, 
current_location_statement_content, 
current_location_statement_type, 
current_location_statement_language, 
nodegroupid, 
current_location
                ) SELECT
                    resourceinstanceid::uuid, 
cur_loc_statement_tileid::uuid, 
cur_loc_statement_current_location_statement_content::jsonb, 
cur_loc_statement_current_location_statement_type::uuid[], 
cur_loc_statement_current_location_statement_language::uuid[], 
cur_loc_statement_nodegroupid::uuid, 
cur_loc_tileid::uuid
                
                FROM staging.rsci_current_location
                WHERE (staging.rsci_current_location.cur_loc_statement_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.rsci_current_location.cur_loc_statement_tileid::uuid IS NOT NULL) AND 
((staging.rsci_current_location.cur_loc_statement_current_location_statement_content::jsonb IS NOT NULL))
                ORDER BY staging.rsci_current_location.resourceinstanceid
                
                ;
                


select * from refresh_geojson_geometries();
