CREATE SCHEMA IF NOT EXISTS "staging";


create or replace function __arches_tile_view_update() returns trigger as $$
declare
    view_namespace text;
    group_id uuid;
    graph_id uuid;
    parent_id uuid;
    tile_id uuid;
    transaction_id uuid;
    json_data json;
    old_json_data jsonb;
    edit_type text;
begin
    select graphid into graph_id from nodes where nodeid = group_id;
    view_namespace = format('%s.%s', tg_table_schema, tg_table_name);
    select obj_description(view_namespace::regclass, 'pg_class') into group_id;
    if (TG_OP = 'DELETE') then
        select tiledata into old_json_data from tiles where tileid = old.tileid;
        delete from resource_x_resource where tileid = old.tileid;
        delete from public.tiles where tileid = old.tileid;
        insert into bulk_index_queue (resourceinstanceid, createddate)
            values (old.resourceinstanceid, current_timestamp) on conflict do nothing;
        insert into edit_log (
            resourceclassid,
            resourceinstanceid,
            nodegroupid,
            tileinstanceid,
            edittype,
            oldvalue,
            timestamp,
            note,
            transactionid
        ) values (
            graph_id,
            old.resourceinstanceid,
            group_id,
            old.tileid,
            'tile delete',
            old_json_data,
            now(),
            'loaded via SQL backend',
            public.uuid_generate_v1mc()
        );
        return old;
    else
        select __arches_get_json_data_for_view(new, tg_table_schema, tg_table_name) into json_data;
        select __arches_get_parent_id_for_view(new, tg_table_schema, tg_table_name) into parent_id;
        tile_id = new.tileid;
        if (new.transactionid is null) then
            transaction_id = public.uuid_generate_v1mc();
        else
            transaction_id = new.transactionid;
        end if;

        if (TG_OP = 'UPDATE') then
            select tiledata into old_json_data from tiles where tileid = tile_id;
            edit_type = 'tile edit';
            if (transaction_id = old.transactionid) then
                transaction_id = public.uuid_generate_v1mc();
            end if;
            update public.tiles
            set tiledata = json_data,
                nodegroupid = group_id,
                parenttileid = parent_id,
                resourceinstanceid = new.resourceinstanceid
            where tileid = new.tileid;
        elsif (TG_OP = 'INSERT') then
            old_json_data = null;
            edit_type = 'tile create';
            if tile_id is null then
                tile_id = public.uuid_generate_v1mc();
            end if;
            insert into public.tiles(
                tileid,
                tiledata,
                nodegroupid,
                parenttileid,
                resourceinstanceid
            ) values (
                tile_id,
                json_data,
                group_id,
                parent_id,
                new.resourceinstanceid
            );
        end if;
        perform __arches_refresh_tile_resource_relationships(tile_id);
        insert into bulk_index_queue (resourceinstanceid, createddate)
            values (new.resourceinstanceid, current_timestamp) on conflict do nothing;
        insert into edit_log (
            resourceclassid,
            resourceinstanceid,
            nodegroupid,
            tileinstanceid,
            edittype,
            newvalue,
            oldvalue,
            timestamp,
            note,
            transactionid
        ) values (
            graph_id,
            new.resourceinstanceid,
            group_id,
            tile_id,
            edit_type,
            json_data::jsonb,
            old_json_data,
            now(),
            'loaded via SQL backend',
            transaction_id
        );
        return new;
    end if;
    end;
$$ language plpgsql;



create or replace function __arches_get_node_value_sql(node nodes) returns text as
$$
declare
    node_value_sql text;
    select_sql     text = '(t.tiledata->>%L)';
    datatype       text = 'text';
begin
    select_sql = format(select_sql, node.nodeid);
    if (node.config ->> 'pgDatatype' is not null) then
        datatype = node.config ->> 'pgDatatype';
    else
        case node.datatype
            when 'geojson-feature-collection' then datatype = 'geometry';
            when 'string' then datatype = 'jsonb';
            when 'number' then datatype = 'numeric';
            when 'boolean' then datatype = 'boolean';
            when 'resource-instance' then datatype = 'jsonb';
            when 'resource-instance-list' then datatype = 'jsonb';
            when 'annotation' then datatype = 'jsonb';
            when 'file-list' then datatype = 'jsonb';
            when 'url' then datatype = 'jsonb';
            when 'date' then datatype = 'timestamp';
            when 'node-value' then datatype = 'uuid';
            when 'domain-value' then datatype = 'uuid';
            when 'domain-value-list' then datatype = 'uuid[]';
            when 'concept' then datatype = 'uuid';
            when 'concept-list' then datatype = 'uuid[]';
            when 'reference' then datatype = 'jsonb';
            else datatype = 'text';
            end case;
    end if;
    case datatype
        when 'geometry' then select_sql = format('
                                st_collect(
                                    array(
                                        select st_transform(geom, 4326) from geojson_geometries
                                        where geojson_geometries.tileid = t.tileid and nodeid = %L
                                    )
                                )',
                                                 node.nodeid
                                          );
        when 'timestamp' then select_sql = format(
                'to_date(
                    t.tiledata->>%L::text,
                    %L
                )',
                node.nodeid,
                node.config ->> 'dateFormat'
                                           );
        when 'uuid[]' then select_sql = format('(
                                    CASE
                                        WHEN t.tiledata->>%1$L is null THEN null
                                        ELSE ARRAY(
                                            SELECT jsonb_array_elements_text(
                                                t.tiledata->%1$L
                                            )::uuid
                                        )
                                    END
                                )', node.nodeid
                                        );
        else null;
        end case;


    node_value_sql = format(
            '%s::%s as "%s"',
            select_sql,
            datatype,
            __arches_slugify(node.name)
                     );
    return node_value_sql;
end
$$ language plpgsql volatile;



UPDATE nodes
SET name = REPLACE(name, '_time-span_edtf', '_time_span_edtf')
WHERE name LIKE '%_time-span_edtf%';

UPDATE nodes
SET name = REPLACE(name, '_time-span_type', '_time_span_type')
WHERE name LIKE '%_time-span_type%';



UPDATE node_groups
SET cardinality = 'n'
WHERE nodegroupid = '2924a04c-73c0-4d09-972e-089a6630e232';



-- FUNCTION: public.__arches_instance_view_update()

-- DROP FUNCTION IF EXISTS public.__arches_instance_view_update();

CREATE OR REPLACE FUNCTION public.__arches_instance_view_update()
    RETURNS trigger
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE NOT LEAKPROOF
AS $BODY$
                declare
                    view_namespace text;
                    model_id uuid;
                    instance_id uuid;
                    transaction_id uuid;
                    edit_type text;
                begin
                    view_namespace = format('%s.%s', tg_table_schema, tg_table_name);
                    select obj_description(view_namespace::regclass, 'pg_class') into model_id;
                    if (TG_OP = 'DELETE') then
                        delete from public.resource_instances where resourceinstanceid = old.resourceinstanceid;
                        insert into bulk_index_queue (resourceinstanceid, createddate)
                            values (old.resourceinstanceid, current_timestamp) on conflict do nothing;
                        insert into edit_log (
                            resourceclassid,
                            resourceinstanceid,
                            edittype,
                            timestamp,
                            note,
                            transactionid
                        ) values (
                            model_id,
                            old.resourceinstanceid,
                            'delete',
                            now(),
                            'loaded via SQL backend',
                            public.uuid_generate_v1mc()
                        );
                        return old;
                    else
                        instance_id = new.resourceinstanceid;
                        if instance_id is null then
                            instance_id = public.uuid_generate_v1mc();
                        end if;

                        if (new.transactionid is null) then
                            transaction_id = public.uuid_generate_v1mc();
                        else
                            transaction_id = new.transactionid;
                        end if;

                        if (TG_OP = 'UPDATE') then
                            edit_type = 'edit';
                            if (transaction_id = old.transactionid) then
                                transaction_id = public.uuid_generate_v1mc();
                            end if;
                            update public.resource_instances
                            set createdtime = new.createdtime,
                                legacyid = new.legacyid
                            where resourceinstanceid = instance_id;
                        elsif (TG_OP = 'INSERT') then
                            edit_type = 'create';
                            insert into public.resource_instances(
                                resourceinstanceid,
                                graphid,
                                legacyid,
                                createdtime,
								 resource_instance_lifecycle_state_id
                            ) values (
                                instance_id,
                                model_id,
                                new.legacyid,
                                now(),
								'f75bb034-36e3-4ab4-8167-f520cf0b4c58'::uuid
                            );
                        end if;
                        insert into bulk_index_queue (resourceinstanceid, createddate)
                            values (instance_id, current_timestamp) on conflict do nothing;
                        insert into edit_log (
                            resourceclassid,
                            resourceinstanceid,
                            edittype,
                            timestamp,
                            note,
                            transactionid
                        ) values (
                            model_id,
                            instance_id,
                            edit_type,
                            now(),
                            'loaded via SQL backend',
                            transaction_id
                        );
                        return new;
                    end if;
                end;
            
$BODY$;

ALTER FUNCTION public.__arches_instance_view_update()
    OWNER TO postgres;



/*
ALTER TABLE TILES DISABLE TRIGGER __arches_check_excess_tiles_trigger;
ALTER TABLE TILES DISABLE TRIGGER __arches_trg_update_spatial_attributes;
*/



    SELECT __arches_create_resource_model_views('d4e956f7-9fad-4fd2-94e3-563a3b2c3585');
    


    SELECT __arches_create_resource_model_views('3dda9f54-d771-11ef-825b-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('36956e50-d770-11ef-8f5d-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('e1d0ea1a-d770-11ef-8c40-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('da0ed58e-d771-11ef-af99-0275dc2ded29');
    


    SELECT __arches_create_resource_model_views('c08bae61-9726-47d8-9751-2dd04d20a7b4');
    

/* ---------------------------------------------------------------------------------- */ 



                INSERT INTO physical_thing.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id, 
transactionid
                ) SELECT
                    
 DISTINCT
                    resourceinstanceid::uuid, 
'f75bb034-36e3-4ab4-8167-f520cf0b4c58'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer, 
transactionid::uuid
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM physical_thing.instances)) AND 
((staging.etl_afs_phys_things.resourceinstanceid::uuid IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.name (
                    resourceinstanceid, 
tileid, 
name_content, 
name_type_, 
transactionid, 
name_language_
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_name_tileid::uuid, 
phys_things_name_name_content::jsonb, 
phys_things_name_name_type_::jsonb, 
phys_things_name_transactionid::uuid, 
phys_things_name_name_language_::jsonb
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_name_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_name_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_name_name_content::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_name_name_language_::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.identifier (
                    resourceinstanceid, 
tileid, 
identifier_content, 
identifier_type, 
transactionid
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_id_tileid::uuid, 
phys_things_id_identifier_content::jsonb, 
phys_things_id_identifier_type::jsonb, 
phys_things_id_transactionid::uuid
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_id_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_id_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_id_identifier_content::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.object_type (
                    resourceinstanceid, 
tileid, 
object_type, 
transactionid
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_obj_type_tileid::uuid, 
phys_things_obj_type_object_type::jsonb, 
phys_things_obj_type_transactionid::uuid
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_obj_type_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_obj_type_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_obj_type_object_type::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.current_location (
                    resourceinstanceid, 
tileid, 
transactionid, 
current_location
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_cur_loc_tileid::uuid, 
phys_things_cur_loc_transactionid::uuid, 

                        coalesce(
                            case jsonb_typeof(phys_things_cur_loc_related_objs) 
                                when 'array' then phys_things_cur_loc_related_objs 
                                else '[]'::jsonb end
                            ) as phys_things_cur_loc_related_objs_1
                        
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_cur_loc_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_cur_loc_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_cur_loc_transactionid::uuid IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_cur_loc_related_objs::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.current_owner (
                    resourceinstanceid, 
tileid, 
transactionid, 
current_owner
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_cur_own_tileid::uuid, 
phys_things_cur_own_transactionid::uuid, 

                        coalesce(
                            case jsonb_typeof(phys_things_cur_own_related_objs) 
                                when 'array' then phys_things_cur_own_related_objs 
                                else '[]'::jsonb end
                            ) as phys_things_cur_own_related_objs_1
                        
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_cur_own_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_cur_own_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_cur_own_transactionid::uuid IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_cur_own_related_objs::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.production_ (
                    resourceinstanceid, 
tileid, 
production_location_geo, 
transactionid, 
production_location, 
production_type, 
production_technique
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_prod_tileid::uuid, 
ST_AsText(ST_GeomFromGeoJSON(phys_things_prod_production_location_geo)), 
phys_things_prod_transactionid::uuid, 

                        coalesce(
                            case jsonb_typeof(phys_things_prod_related_objs) 
                                when 'array' then phys_things_prod_related_objs 
                                else '[]'::jsonb end
                            ) as phys_things_prod_related_objs_1
                        , 
phys_things_prod_production_type::jsonb, 
phys_things_prod_production_technique::jsonb
                
                FROM staging.etl_afs_phys_things
                WHERE (staging.etl_afs_phys_things.phys_things_prod_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things.phys_things_prod_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things.phys_things_prod_production_location_geo::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_prod_related_objs::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_prod_production_type::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things.phys_things_prod_production_technique::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things.resourceinstanceid
                
                ;
                

/* ---------------------------------------------------------------------------------- */ 



                INSERT INTO physical_thing.instances (
                    resourceinstanceid, 
resource_instance_lifecycle_state_id, 
graphid, 
graphpublicationid, 
principaluser_id, 
transactionid
                ) SELECT
                    
 DISTINCT
                    resourceinstanceid::uuid, 
'f75bb034-36e3-4ab4-8167-f520cf0b4c58'::uuid AS state_id, 
graphid::uuid, 
graphpublicationid::uuid, 
principaluser_id::integer, 
transactionid::uuid
                
                FROM staging.etl_afs_phys_things_state_dim
                WHERE (staging.etl_afs_phys_things_state_dim.resourceinstanceid NOT IN (SELECT resourceinstanceid FROM physical_thing.instances)) AND 
((staging.etl_afs_phys_things_state_dim.resourceinstanceid::uuid IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things_state_dim.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.statement (
                    resourceinstanceid, 
tileid, 
statement_content, 
transactionid, 
statement_type, 
statement_language_
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_state_cont_tileid::uuid, 
phys_things_state_cont_statement_content::jsonb, 
phys_things_state_cont_transactionid::uuid, 
phys_things_state_cont_statement_type::jsonb, 
phys_things_state_cont_statement_language_::jsonb
                
                FROM staging.etl_afs_phys_things_state_dim
                WHERE (staging.etl_afs_phys_things_state_dim.phys_things_state_cont_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things_state_dim.phys_things_state_cont_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things_state_dim.phys_things_state_cont_statement_content::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_state_cont_statement_type::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_state_cont_statement_language_::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things_state_dim.resourceinstanceid
                
                ;
                


                INSERT INTO physical_thing.dimension (
                    resourceinstanceid, 
tileid, 
dimension_type_, 
transactionid, 
dimension_value_, 
dimension_lowest_possible_value, 
dimension_highest_possible_value, 
dimension_unit
                ) SELECT
                    
                    resourceinstanceid::uuid, 
phys_things_dim_tileid::uuid, 
phys_things_dim_dimension_type_::jsonb, 
phys_things_dim_transactionid::uuid, 
phys_things_dim_dimension_value_::numeric, 
phys_things_dim_dimension_lowest_possible_value::numeric, 
phys_things_dim_dimension_highest_possible_value::numeric, 
phys_things_dim_dimension_unit::jsonb
                
                FROM staging.etl_afs_phys_things_state_dim
                WHERE (staging.etl_afs_phys_things_state_dim.phys_things_dim_tileid::uuid NOT IN (SELECT tileid FROM tiles)) AND 
(staging.etl_afs_phys_things_state_dim.phys_things_dim_tileid::uuid IS NOT NULL) AND 
((staging.etl_afs_phys_things_state_dim.phys_things_dim_dimension_type_::jsonb IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_dim_dimension_value_::numeric IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_dim_dimension_lowest_possible_value::numeric IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_dim_dimension_highest_possible_value::numeric IS NOT NULL) OR (staging.etl_afs_phys_things_state_dim.phys_things_dim_dimension_unit::jsonb IS NOT NULL))
                
                ORDER BY staging.etl_afs_phys_things_state_dim.resourceinstanceid
                
                ;
                


/*
ALTER TABLE TILES ENABLE TRIGGER __arches_check_excess_tiles_trigger;
ALTER TABLE TILES ENABLE TRIGGER __arches_trg_update_spatial_attributes;
*/



select * from refresh_geojson_geometries();
